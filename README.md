# VidChopper

VidChopper is a Windows desktop application for splitting a source video into chapter clips with `ffmpeg`, using a Qt 6 interface optimized for fast local execution.

The repository is being built as a production-ready portfolio project with staged commits, automated tests, and documentation that explains the design and operational tradeoffs behind the application.
